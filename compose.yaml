services:
  platformer:
    build:
      context: .
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "8080"
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
